# fips project yml file
---

# external dependencies
imports:
    # short-name from registry, or git url
    fips-hello-dep1:
        # imported items, left is exported name, right is local name
        dep1:   dep1
        dep2:   dep2


